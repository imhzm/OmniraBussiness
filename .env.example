# Lead-capture endpoint for the contact + newsletter forms.
# Leave empty to use the WhatsApp / local-confirmation fallback (default).
# Set to a real CRM webhook or API route URL to capture leads on go-live.
NEXT_PUBLIC_LEAD_ENDPOINT=
